import Calendar from "./hooks/date-picker";
import PhoneNumber from "./hooks/phone-number";
import Clipboard from "./hooks/clipboard";

let Hooks = {
  Calendar: Calendar,
  PhoneNumber: PhoneNumber,
  Clipboard: Clipboard,
};

export default Hooks;
